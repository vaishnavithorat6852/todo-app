# todo-app
A simple Todo List web app built with HTML, CSS and JavaScript. Features include adding, completing and deleting tasks, filter by All / Active / Completed, localStorage persistence and a clean responsive UI. 
